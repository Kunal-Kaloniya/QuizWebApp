import { useNavigate } from "react-router-dom";

function IntroPage() {

    const navigate = useNavigate();

    return (
        <div>
            <div className="w-full min-h-[90vh] bg-gray-500">

            </div>
        </div>
    );
}

export default IntroPage;